syntax = "proto3";

package chat;

option go_package = "chatRoom/;proto";

message ChatMessage {
  string body = 1;
}

service ChatService {
  rpc Chat(stream ChatMessage) returns (stream ChatMessage);
}
